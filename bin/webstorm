#!/bin/bash
#Generated by JetBrains Toolbox 2.5.2.35332 at 2025-01-17T15:12:47

declare -a intellij_args=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    intellij_args+=("\"$o\"")
  else
    intellij_args+=("$o")
  fi
done

open -na "/Applications/WebStorm.app/Contents/MacOS/webstorm" $wait --args "${intellij_args[@]}"